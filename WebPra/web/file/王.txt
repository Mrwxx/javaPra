一个句子在另一个句子中的位置
a + b = c， a，c相似
b可以为空，a.length() <= c.length()
// 首先判断两个句子的长度
// 再判断长度小的a在c中的位置
将每个字符串中的单词都取出来，比较
a 的所有单词都在c数组前面或后面
或者一部分在前，一部分在后
// 1. a在c的左侧， a是c的子串，第一个相等，最后一个不等
// 2. a在c的右侧    a是c的子串，第一个不等，最后一个相等
// 3. a拆开后在c的两侧
a都在c的数组中，且当a结束时c也结束了
a结束时c还没有结束，判断

nums[i] - rev(nums[i]) 是相等的
求出数组中每个值的rev，得到新的数组
5个数，10
4个数，6
3个数，3